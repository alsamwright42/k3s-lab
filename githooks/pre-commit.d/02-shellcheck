#!/usr/bin/env bash
set -euo pipefail

# Directory Anchoring matching project standard (Explicit  array index)
HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${HOOK_DIR}/../.." && pwd)"

"${REPO_ROOT}/scripts/workstation/audit-shellcheck.sh"
