#!/bin/sh
# .git/hooks/post-commit
#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
rclone sync docs remote:"HomeLab Operations/docs" >/dev/null 2>&1 &
