# Kubernetes Manifests

This directory contains declarative Kubernetes resources for the K3s cluster.

- `base/`: Core infrastructure components (namespaces, CRDs, RBAC, storage classes).
- `apps/`: Application workloads and service definitions.
